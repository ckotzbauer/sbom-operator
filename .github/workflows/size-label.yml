name: size-label
on:
  pull_request_target:
    types: [opened, reopened, synchronize]

jobs:
  size-label:
    uses: ckotzbauer/actions-toolkit/.github/workflows/toolkit-size-label.yml@d5d51e78c95c555c23d5ec484a5a32aa7d02616e # 0.51.0
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}

