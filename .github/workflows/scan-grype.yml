name: scan-grype

on:
  schedule:
    - cron: "0 12 * * 1"
  workflow_dispatch: {}

permissions:
  actions: read
  contents: read
  packages: read
  security-events: write

jobs:
  scan-scan:
    uses: ckotzbauer/actions-toolkit/.github/workflows/toolkit-scan.yml@fd699521fc995525d339964a82f6f781581bf471 # 0.57.0
    with:
      docker-tag: ghcr.io/ckotzbauer/sbom-operator:latest
