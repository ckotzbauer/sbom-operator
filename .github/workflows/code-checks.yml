name: code-checks

on:
  pull_request: {}
  push:
    branches:
      - "**"

jobs:
  gosec:
    uses: ckotzbauer/actions-toolkit/.github/workflows/toolkit-lint.yml@0.37.2
    with:
      install-go: true
      go-version: "1.21.4"
      lint-commands: |
        make bootstrap-tools
        make lintsec

  golint:
    uses: ckotzbauer/actions-toolkit/.github/workflows/toolkit-lint.yml@0.37.2
    with:
      install-go: true
      go-version: "1.21.4"
      lint-commands: |
        make bootstrap-tools
        make lint
