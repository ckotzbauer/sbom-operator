name: stale
on:
  schedule:
  - cron: "0 0 * * *"

jobs:
  stale:
    uses: ckotzbauer/actions-toolkit/.github/workflows/toolkit-stale.yml@d5d51e78c95c555c23d5ec484a5a32aa7d02616e # 0.51.0
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
