name: stale
on:
  schedule:
  - cron: "0 0 * * *"

jobs:
  stale:
    uses: ckotzbauer/actions-toolkit/.github/workflows/toolkit-stale.yml@v0.42.1
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
